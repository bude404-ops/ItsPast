# Public frontend configuration only. Never commit secret keys.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
VITE_MAP_STYLE_URL=https://tiles.example.com/style.json
VITE_MAP_PUBLIC_TOKEN=YOUR_PUBLIC_MAP_TOKEN
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_PLACEHOLDER

# Server-side only. Do not expose in Vite frontend variables.
# Supabase service role key placeholder belongs in deployment secrets only.
# Stripe secret key placeholder belongs in deployment secrets only.
# AI provider secret key placeholder belongs in deployment secrets only.
# Database URL placeholder belongs in deployment secrets only.
