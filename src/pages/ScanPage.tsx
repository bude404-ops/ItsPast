import { Link } from 'react-router-dom';
import { demoEntities } from '../data/demoEntities';

export function ScanPage() {
  return <section className="space-y-5"><div className="rounded-3xl border border-stone-800 bg-stone-900 p-5"><p className="text-xs font-bold uppercase tracking-[0.24em] text-sepia-400">Scan workflow</p><h1 className="mt-2 text-4xl font-black">SCAN ENGINE COMING ONLINE</h1><p className="mt-3 text-stone-300">Camera recognition is not claimed yet. Use manual discovery while computer vision, GPS matching, and location candidates are integrated.</p><div className="mt-5 rounded-3xl border border-dashed border-stone-700 bg-stone-950 p-8 text-center text-stone-400">Camera placeholder · location lookup · manual selection</div></div><div className="space-y-3"><h2 className="text-xl font-bold">Nearby candidate records</h2>{demoEntities.map((entity) => <Link key={entity.id} to={`/entity/${entity.id}`} className="block rounded-2xl border border-stone-800 bg-stone-900 p-4"><div className="flex justify-between gap-3"><strong>{entity.name}</strong><span className="text-sepia-400">{entity.confidenceLevel}</span></div><p className="text-sm text-stone-400">{entity.location.address}</p></Link>)}</div></section>;
}
