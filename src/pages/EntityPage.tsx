import { Link, useParams } from 'react-router-dom';
import { ConfidenceBadge } from '../components/ConfidenceBadge';
import { useEntityBundle } from '../hooks/useEntityData';
import { useSavedDiscoveries } from '../hooks/useSavedDiscoveries';

export function EntityPage() {
  const { id } = useParams();
  const { entity, timeline, sources, reconstruction, loading } = useEntityBundle(id);
  const saved = useSavedDiscoveries();
  if (loading) return <section className="rounded-3xl border border-stone-800 bg-stone-900 p-5 text-stone-300">Reading the record…</section>;
  if (!entity) return <section className="rounded-3xl border border-stone-800 bg-stone-900 p-5"><h1 className="text-3xl font-black">Record not found</h1><Link to="/explore" className="mt-4 inline-block rounded-full bg-sepia-400 px-4 py-2 font-bold text-stone-950">Back to explore</Link></section>;
  return <article className="space-y-5"><section className="rounded-3xl border border-stone-800 bg-stone-900 p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.24em] text-sepia-400">Physical entity</p><h1 className="mt-2 text-4xl font-black">{entity.name}</h1></div>{entity.demoData && <span className="rounded-full border border-stone-700 px-2 py-1 text-xs text-stone-300">DEMO DATA</span>}</div><p className="mt-3 text-stone-300">{entity.description}</p><div className="mt-4 grid grid-cols-2 gap-2 text-sm"><div className="rounded-2xl bg-stone-950 p-3"><span className="text-stone-500">Status</span><strong className="block">{entity.currentStatus}</strong></div><div className="rounded-2xl bg-stone-950 p-3"><span className="text-stone-500">Location</span><strong className="block">{entity.location.address}</strong></div><div className="rounded-2xl bg-stone-950 p-3"><span className="text-stone-500">Established</span><strong className="block">{entity.createdDate ?? 'Unknown'}</strong></div><div className="rounded-2xl bg-stone-950 p-3"><span className="text-stone-500">Data quality</span><strong className="block text-sepia-400">{entity.dataQuality}%</strong></div></div><div className="mt-4 flex flex-wrap gap-2"><ConfidenceBadge level={entity.confidenceLevel} /><button onClick={() => saved.toggle(entity.id)} className="rounded-full bg-sepia-400 px-3 py-1 text-sm font-bold text-stone-950">{saved.isSaved(entity.id) ? 'Saved' : 'Save discovery'}</button></div><div className="mt-5 grid grid-cols-2 gap-2"><Link to={`/timeline/${entity.id}`} className="rounded-2xl bg-sepia-400 px-4 py-3 text-center font-bold text-stone-950">EXPLORE ITS PAST</Link><Link to="/scan" className="rounded-2xl border border-stone-700 px-4 py-3 text-center font-bold">Scan nearby</Link></div></section><section className="rounded-3xl border border-stone-800 bg-stone-900 p-5"><h2 className="text-2xl font-bold">Timeline preview</h2>{timeline.length ? timeline.slice(0,3).map((event) => <div key={event.id} className="mt-3 border-l border-sepia-400 pl-3"><p className="text-xs text-sepia-400">{event.date}</p><h3 className="font-bold">{event.title}</h3><p className="text-sm text-stone-400">{event.description}</p></div>) : <p className="mt-3 text-sm text-stone-400">No verified timeline events yet.</p>}</section><section className="rounded-3xl border border-stone-800 bg-stone-900 p-5"><h2 className="text-2xl font-bold">Sources</h2>{sources.length ? sources.map((source) => <div key={source.id} className="mt-3 rounded-2xl bg-stone-950 p-3"><p className="text-xs text-sepia-400">{source.sourceType}</p><h3 className="font-bold">{source.title}</h3><p className="text-sm text-stone-400">{source.description}</p></div>) : <p className="mt-3 text-sm text-stone-400">No public sources linked yet.</p>}</section>{reconstruction && <section className="rounded-3xl border border-stone-800 bg-stone-900 p-5"><h2 className="text-2xl font-bold">Reconstruction request</h2><p className="mt-2 text-stone-400">Target year {reconstruction.targetYear} · {reconstruction.status}</p><ConfidenceBadge level={reconstruction.confidence} /></section>}</article>;
}
